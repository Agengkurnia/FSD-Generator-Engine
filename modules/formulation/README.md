# Formulation (Combined)

| Path | Isi |
|------|-----|
| `build_combined.py` | FSD gabungan (embedded MD legacy) |
| `scripts/` | `build_formula_fsd.py`, `gen_combined_fsd.py`, dll. |
| `html_diagrams/` | PNG flow/state diagram |

**Lab Formula per tipe:** `modules/formulation-lab/` (`py build.py`)

Folder lama `Formulation/` di root — hanya README redirect.
